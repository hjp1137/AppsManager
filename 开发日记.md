# 开发日记

## 2026-07-16
### 工程事实与变更
1. **项目初始化**：新建 .NET 9.0 WPF 应用程序，确立 MVVM 代码目录结构。
2. **多启动项（SubService）重构**：
   * 创建 `SubService` 模型，包含独立 ID、工作路径、启动命令、调试端口、状态及日志流。
   * 重构 `ProjectItem`，使用 `ObservableCollection<SubService>`，自动汇总各子服务状态。
   * 重构 `ProjectDetector`，实现 2 层子目录深度扫描（排除 .git/node_modules 等），自动生成多启动子项。
   * 重构 `ProcessService` 和 `MainViewModel`，将控制指令和事件分发下沉至子服务级。
3. **识别精度与解析器升级（用户反馈优化）**：
   * **.NET 过滤**：优化 `ProjectDetector`，读取 `.csproj` 文件仅保留包含 Web/Worker SDK 或 OutPutType 为 Exe/WinExe 的项目，成功屏蔽无效的类库（Class Library）项目。
   * **快捷粘贴解析**：在 `ProjectDetector` 中实现 `ParseCommand` 方法。利用正则提取命令行中的 `cd` 路径（转换为绝对路径）和执行命令（剔除 `cd` 语句及 `&&` 连接符），并自动分析提取端口（如 `--port 5555` 等）。
4. **日志美化与 Tab 切换升级（用户反馈优化）**：
   * **ANSI 彩色终端**：创建 `AnsiColorParser` 静态解析器，支持将带有 ANSI 转义码（如 `\x1b[32m` 等）的行解析为 WPF FlowDocument 的 `Run` 段落，进行前景色渲染（红色、绿色、蓝色、黄色、青色等）和粗体样式绑定；并将原 TextBox 升级为 `RichTextBox`；MainWindow 实时监听日志流事件在 UI 线程增量渲染追加，并设段落数量上限 1000 行实施自清洁防爆内存。
   * **页签 Tab 切换与 ControlTemplate 重构**：用 TabControl 完全代替 ComboBox。通过定义 `TabItem` 的 `ControlTemplate`，彻底剥离 Windows 默认的高亮白底模板；在被选中（IsSelected=True）时显式设置其 Foreground 为高亮纯白（`#FFFFFF`），背景为 `#2D2D2D`，底边为绿色激活线，解决系统默认底色导致的“白底白字”阅读障碍。
5. **四色状态指示灯与拉伸自适应（用户反馈优化）**：
   * **四色状态小圆点与 StatusBrush**：升级 `SubService` 绑定机制，在 Model 里提供 `System.Windows.Media.Brush` 类型的 `StatusBrush`，避开 string 隐式类型转换刷新障碍，保证 WPF 渲染引擎 100% 重绘。灯包含四色：绿色（运行中/Running）、黄色（正在启动中/Starting）、红色（已停止且有运行日志）、灰色（Stopped 且无日志，即未启动过的干净状态），当用户清空日志时颜色可自动智能恢复为灰色。
4. **纯系统命令原生调试、原子并发防重、项目分组管理、PE高清图标与停止回显**：
   * 侧边栏分组把手升级为高清高亮 SVG 矢量点阵图标：将暗色点阵升级为独立的 6 散点 SVG 矢量把手，颜色提升为明亮银灰蓝（`#94a3b8` / `#60a5fa`），彻底消除暗黑背景下融入看不清的问题。
   * 应用程序版本号规范化升级：顶部 Header 的版本标识规范更新为 `v1.0`。
   * 侧边栏分组数量绝对垂直对齐：重构分组 Item 结构，将编辑/删除操作按钮收拢为 hover 显现，右侧 `(N)` 数量徽章统一采用固定基准排版，所有分组数量无论在 hover 与否均 100% 垂直精确对齐。
   * 卡片拖拽移入分组：卡片支持原生 HTML5 拖拽（`draggable`），当拖拽卡片至侧边栏任一目标分组（如“默认”、“测试”等）时触发亮蓝色高亮反馈，松手即自动修改该项目所属分组并持久化同步。
   * 自定义分组拖拽排序：“全部项目”与“默认”固定在最上方，自定义分组（如“测试”、“自用码”等）支持抓取把手自由拖拽上下排序，拖拽调整后即时持久化至 LocalStorage。

   * 日志单行单网址严格起始位置对齐与青色高亮：将同一行多个网址拆分为单行展示（`本地 IP 访问:`、`本地域名访问:`、`局域网访问:`），中文与字符按等宽格式精准对齐至同一列；链接文字颜色统一使用原本清晰的青色（`\x1b[36m`）。
   * 彻底解决 xterm 终端超链接下划线向左偏移问题：重构 `registerLinkProvider` 坐标算法，通过 `line.getCell(col)` 遍历并累加全角 CJK 中文字符宽度，将 JS 字符串下标准确映射为 1-based 终端 Cell 网格坐标，实现下划线与 URL 像素级完美重合。
   * 日志标题行展示所有可用链接标签：终端顶部工具栏同步展示当前选定服务的所有可用访问地址（`127.0.0.1`、`localhost`、局域网 IP），支持点击直接在浏览器新标签页打开。
   * 项目分组全生命周期管理与编辑弹窗实时同步：`customGroups` 采用 LocalStorage 本地持久化，编辑/新建弹窗采用暗黑定制下拉选择器与【➕ 新建分组】按钮，新增分组 100% 实时出现在选择器中并自动选中。
   * 彻底解决重启后日志重复两次竞态：在 `ProcessManager` 中引入原子互斥锁标志位 `takeoverInjected`，在探测网络前先行加锁判定，杜绝并发请求双重注入 RingBuffer。
   * SMT.Exam 纯系统命令原生编译与调试（零外部依赖）：补齐项目 Uni-App Webpack 4 与 Babel 依赖，配置 `dev:h5` 纯系统命令启动（`cross-env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=development UNI_PLATFORM=h5 UNI_INPUT_DIR=. UNI_CLI_CONTEXT=. vue-cli-service uni-serve`），在 8080 端口成功编译启动并提供服务，移除第三方 HBuilderX 依赖。
   * AppsManager.exe 嵌入专属高清应用图标：生成 256x256 至 16x16 全套现代科技发光闪电 `.ico`，通过 `app.syso` 资源文件嵌入 Go 编译器，使 Windows 资源管理器、任务栏呈现专属高清现代图标。
   * 停止服务与退出日志回显：点击【⏹ 停止】或子进程退出后，在终端日志中输出显目的红色分割提示：`⏹  [AppsManager] 服务已正式停止 (进程已退出，端口 :xxxx 已完全释放)`。
   * 彻底修复端口被自动改回问题：日志嗅探仅在 `svc.Port <= 0` 时补充，已配置端口获得最高优先级保护，用户修改的 5001 端口永不被历史日志篡改覆盖。



   * 终端日志 URL 原生点击跳转：为 xterm.js 注册 `registerLinkProvider`，日志中所有 HTTP/HTTPS 地址悬浮下划线高亮，点击直达默认浏览器打开。
   * 升级经典 SVG 复制图标 & 紧贴路径排版：卡片项目名称与物理路径采用经典双层矩形 SVG 图标，路径复制按钮紧贴文字；复制触发全局非阻塞 Toast 提示。
   * 建立真实端口活跃态无缝接管机制：启动与轮询若检测到端口已被占用，精准识别为“有效运行中”并展示【停止】按钮；自动执行 HTTP GET 健康探测心跳（带毫秒耗时），抓取 PID 和进程名输出彩色诊断回显；
   * 启动与控制台访问地址直达：服务启动及接管时在日志中清晰打印本地与局域网访问 URL；卡片端口徽章与终端工具栏均配有可点击直接打开浏览器的链接徽章；
   * 点击【停止】时同步触发端口进程多重强杀；点击【启动】时自动在启动前 100ms 快速清理残留端口，彻底根除 `EADDRINUSE` 报错。
   * 构建模块化规则配置管理器（`core/rules_config.go`），实现 `%LOCALAPPDATA%\AppsManager\detection_rules.json` 持久化读写与出厂默认预置规则。
   * 新增独立前端规则管理组件（`RulesManager.vue`）并在顶部 Header 提供无缝视图切换，支持规则的实时检索、分类筛选、新增、编辑、删除、一键开关及恢复出厂默认。
   * 优化新建/编辑规则弹窗为 680px 居中大弹窗与毛玻璃半透明遮罩，修复样式类名与表单排版。
   * 修复子服务路径继承缺陷，在服务端保存及启动调度层全面保证子服务继承项目物理路径，彻底解决找不到 `package.json`（ENOENT）的问题。
   * 新增 `/api/system/select_folder` 接口，支持在弹窗中点击【📁 选择文件夹】唤起 Windows 原生文件夹选择对话框，自动回填路径、提取项目名并触发子启动项智能识别。
   * 新增 `/api/system/exit` 退出接口并在界面右上方增设【🛑 退出程序】按钮，点击可一键安全关停所有子服务并彻底退出。
5. **自验证测试**：
   * 编写 `core/core_test.go` 并执行 `go test -v ./core`，RingBuffer、命令解析、日志嗅探、端口探测、穷举识别规则、规则配置管理器 6 项测试全部通过（0.568s）。
   * 编写 `test_api.ps1` 与 `test_rules_api.ps1`，成功验证静态首页加载、多项目读取、规则查询/新增/修改/重置及 `/api/system/exit` 优雅退出链路。
6. **被选中项目卡片高亮（用户反馈优化）**：
   * 在 `ProjectItem` 中加入 `IsSelected` 属性，并在 `MainViewModel` 的 `SelectedProject` 改变时同步更新各卡片选中状态。
   * 在 `MainWindow.xaml` 里的卡片 Border 样式中配置 `DataTrigger`，一旦被选中则将卡片外框变粗并亮起绿色（`#4CAF50`），背景稍微变浅（`#2D2D2D`）。解决了多项目并存时，日志窗口所属项目不够直观明晰的问题。
7. **一键强杀冲突端口（新功能开发）**：
   * 在 `MainViewModel.cs` 中新增公有命令 `KillPortCommand` 及其异步实现 `KillPortConflict(SubService)`。
   * 调用 `PortService.KillProcessOnPort` 对占用该端口的孤儿残留进程进行强杀，释放网络资源。
   * 在子服务的终端日志流中实时打印有色状态日志，告知强杀开始及释放完成。
   * 在 `MainWindow.xaml` 每个子服务控制按钮区（▶/■）右侧，新增橙色 ⚡ (闪电) 图标强杀按钮；通过 DataTrigger 在端口 `Port = 0`（即 Worker）时全自动隐藏此按钮。
8. **非阻塞异步停止优化与重启子服务（用户反馈及新功能）**：
   * **非阻塞异步停止**：将 `ProcessService.StopSubService` 内进程 `Kill` 及端口 `netstat` 释放重度阻塞操作全面包装为后台 `Task.Run` 运行，防范同步阻塞死锁 WPF 主线程。
   * **全局停止重构**：将全局一键停止所有 `StopAllGlobalCommand` 重新实现为遍历所有子服务异步发起 `StopSubService`，彻底消除了同步强杀导致的窗口崩溃或死掉。
   * **一键重启子服务**：在 `MainViewModel.cs` 中新增 `RestartSubServiceCommand` 及其异步流水线实现 `RestartSubService(SubService)`：发起异步停止 -> 轮询等待状态完全退回为 `Stopped` -> 预留 300ms 保证端口完全解绑 -> 再次拉起 `StartSubService`。
   * **UI 按钮装配**：在 `MainWindow.xaml` 的停止按钮 ■ 右侧配装深青色 ↻ 重启按钮。
9. **编译与自测试**：
   * 在 `App.xaml.cs` 中增加 `--test` 命令行诊断入口，加入“测试 3（复合命令粘贴解析断言）”、“测试 4（ANSI 彩色控制台转义解析断言）”及“测试 5（带 ANSI 转义码的日志智能端口匹配提取断言）”逻辑。
   * 修复 XAML 误用的 `2000` 命名空间为 `2006`，解决 ToolTip 转义 `&` 为 `&amp;` 的 XML 解析报错，消除 warnings。
   * 运行 `dotnet run -- --test` 成功输出“所有自诊断自测试全部成功！核心逻辑验证无误。”，并在 `bin/Debug/net9.0-windows/test_result.log` 中保留测试日志。项目实现 0 错误 0 警告顺利构建。

## 2026-08-18
### 工程事实与变更
1. **架构轻量化重构 (Go + Webview2 / xterm.js)**：
   * 针对原 .NET WPF 依赖重（80~120MB+）、大量 Log 刷屏卡死 UI 线程、以及多开时单项卡死拖垮全局的问题，重构为 Go 核心引擎 + 现代 Web 前端。
   * 使用 `-H windowsgui` 参数编译输出单文件可执行程序 `AppsManager.exe`（7.10 MB），彻底去除黑色的命令行控制台窗口，双击静默运行。
   * 闲置内存 < 25MB，目标机零运行环境依赖。
2. **协程物理隔离与防假死**：
   * 每个 SubService 运行在专属的独立 Goroutine 与 Context 中，单服务假死不影响其他服务和主界面。
   * 端口探测改用 Go 原生 Net Dial 毫秒级探测，彻底淘汰同步 `cmd netstat` 阻塞。
3. **日志流控、硬件加速终端、抽屉上下拉伸高度调节与项目名标题**：
   * 后端实现定长 Ring Buffer（容量 3000 行）防止内存泄露，并以 50ms 聚合窗口批量推送日志。
   * 前端集成 VS Code 同款 `xterm.js`，采用 Canvas 硬件加速渲染，彻底解决海量日志刷屏导致的排版卡死。
   * 终端抽屉顶部边框增设 `.drawer-resizer` 拉伸手柄，支持鼠标上下拖拽自由调节高度（100px ~ 800px+）并由 `ResizeObserver` 自动重绘画布。
   * 终端 Tab 与工具栏标题清晰呈现为 `项目名称 · 子服务名称`。
4. **HBuilderX原生接入、系统调用接口补全、接管日志防重、停止日志回显与端口保护**：
   * HBuilderX 原生调试环境无缝接入：解决 SMT.Exam 依赖 HBuilderX 内置编译器无法脱离运行的问题；卡片增设【🟢 HBuilderX】专属按钮；新增 `/api/system/open_hbuilderx`、`/api/system/open_folder`、`/api/system/open_code` 系统级唤起接口；启动命令配置为通过 HBuilderX CLI 原生打开项目并在运行后自动接管 8080 端口。
   * 接管日志彻底防重：优化后端 RingBuffer 注入与前端 `fetchLogs` 链路，消除 WebSocket 广播与 HTTP 叠加导致的日志重复显示两遍问题。
   * 停止服务与退出日志回显：点击【⏹ 停止】或子进程退出后，在终端日志中输出显目的红色分割提示：`⏹  [AppsManager] 服务已正式停止 (进程已退出，端口 :xxxx 已完全释放)`。
   * 彻底修复端口被自动改回问题：日志嗅探仅在 `svc.Port <= 0` 时补充，已配置端口获得最高优先级保护，用户修改的 5001 端口永不被历史日志篡改覆盖。
   * 终端日志 URL 原生点击跳转：为 xterm.js 注册 `registerLinkProvider`，日志中所有 HTTP/HTTPS 地址悬浮下划线高亮，点击直达默认浏览器打开。
   * 升级经典 SVG 复制图标 & 紧贴路径排版：卡片项目名称与物理路径采用经典双层矩形 SVG 图标，路径复制按钮紧贴文字；复制触发全局非阻塞 Toast 提示。
   * 建立真实端口活跃态无缝接管机制：启动与轮询若检测到端口已被占用，精准识别为“有效运行中”并展示【停止】按钮；自动执行 HTTP GET 健康探测心跳（带毫秒耗时），抓取 PID 和进程名输出彩色诊断回显；
   * 启动与控制台访问地址直达：服务启动及接管时在日志中清晰打印本地与局域网访问 URL；卡片端口徽章与终端工具栏均配有可点击直接打开浏览器的链接徽章；
   * 点击【停止】时同步触发端口进程多重强杀；点击【启动】时自动在启动前 100ms 快速清理残留端口，彻底根除 `EADDRINUSE` 报错。
   * 构建模块化规则配置管理器（`core/rules_config.go`），实现 `%LOCALAPPDATA%\AppsManager\detection_rules.json` 持久化读写与出厂默认预置规则。
   * 新增独立前端规则管理组件（`RulesManager.vue`）并在顶部 Header 提供无缝视图切换，支持规则的实时检索、分类筛选、新增、编辑、删除、一键开关及恢复出厂默认。
   * 优化新建/编辑规则弹窗为 680px 居中大弹窗与毛玻璃半透明遮罩，修复样式类名与表单排版。
   * 修复子服务路径继承缺陷，在服务端保存及启动调度层全面保证子服务继承项目物理路径，彻底解决找不到 `package.json`（ENOENT）的问题。
   * 新增 `/api/system/select_folder` 接口，支持在弹窗中点击【📁 选择文件夹】唤起 Windows 原生文件夹选择对话框，自动回填路径、提取项目名并触发子启动项智能识别。
   * 新增 `/api/system/exit` 退出接口并在界面右上方增设【🛑 退出程序】按钮，点击可一键安全关停所有子服务并彻底退出。








5. **自验证测试**：
   * 编写 `core/core_test.go` 并执行 `go test -v ./core`，RingBuffer、命令解析、日志嗅探、端口探测、穷举识别规则、规则配置管理器 6 项测试全部通过（0.548s）。
   * 编写 `test_api.ps1` 与 `test_rules_api.ps1`，成功验证静态首页加载、多项目读取、规则查询/新增/修改/重置及 `/api/system/exit` 优雅退出链路。






